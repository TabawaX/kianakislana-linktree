{
  "version": 2,
  "builds": [
    {
      "src": "app.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/api/ip", "dest": "/app.js" },
    { "handle": "filesystem", "src": "/(.*)", "dest": "/" }
  ]
}
